package items

templ ViewItems(items []Item) {
	<div>
		<ul>
			for _, item := range items {
				<li>{ item.Name }</li>
			}
		</ul>
	</div>
}

templ ViewItem(item Item) {
	<div>
		{ item.Name }
		{ item.Quantity }
	</div>
}
